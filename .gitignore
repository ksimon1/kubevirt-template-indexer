# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Golang stuff
# # all-in-one YAML
cluster/template-indexer.yaml
cmd/kubevirt-template-indexer/kubevirt-template-indexer
vendor/
Gopkg.lock
